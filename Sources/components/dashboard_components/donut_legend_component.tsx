import React from 'react';
import { Image, Text, StyleSheet, Dimensions, ImageSourcePropType, View  } from 'react-native';

type DonutLegend = {
    color: string[],
    value: {string:string}
}

const Dot: React.FC<DonutLegend> = ({ color }) => {



    return (
        <>
        </>
    );
}